const username = "Radoslav";
// username = "something else";

// var result;
function add(a: number, b: number) {
  let result = a + b;
  return result;
}

const age = 26;

if (age > 20) {
  var isOld = true;
}

// console.log(isOld); // avaivable outside if scope block
